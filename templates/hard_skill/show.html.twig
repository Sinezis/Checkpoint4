{% extends 'base.html.twig' %}

{% block title %}HardSkill
{% endblock %}

{% block body %}
	<h1>HardSkill</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ hard_skill.id }}</td>
			</tr>
			<tr>
				<th>Image</th>
				<td>{{ hard_skill.image }}</td>
			</tr>
			<tr>
				<th>Nom</th>
				<td>{{ hard_skill.nom }}</td>
			</tr>
			<tr>
				<th>Description</th>
				<td>{{ hard_skill.description }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_hard_skill_index') }}">back to list</a>

	<a href="{{ path('app_hard_skill_edit', {'id': hard_skill.id}) }}">edit</a>

	{{ include('hard_skill/_delete_form.html.twig') }}
{% endblock %}
