{% extends 'base.html.twig' %}

{% block title %}SoftSkill
{% endblock %}

{% block body %}
	<h1>SoftSkill</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ soft_skill.id }}</td>
			</tr>
			<tr>
				<th>Nom</th>
				<td>{{ soft_skill.nom }}</td>
			</tr>
			<tr>
				<th>Description</th>
				<td>{{ soft_skill.description }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_soft_skill_index') }}">back to list</a>

	<a href="{{ path('app_soft_skill_edit', {'id': soft_skill.id}) }}">edit</a>

	{{ include('soft_skill/_delete_form.html.twig') }}
{% endblock %}
